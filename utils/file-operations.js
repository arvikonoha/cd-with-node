module.exports.read = async filepath => {
  return new Promise((resolve, reject) => {
    // run the resolve funtion with text in the file , ex: resolve(text)
    // run reject({type:"file",error:err}) in the case of error
  });
};
